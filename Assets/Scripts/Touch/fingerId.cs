using UnityEngine;

public class fingerId : MonoBehaviour
{
    public int fingerNumber;
    //public List<int> 手指編號 = new List<int>();
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
