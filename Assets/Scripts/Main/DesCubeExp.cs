using UnityEngine;

public class DesCubeExp : MonoBehaviour
{
    public GameObject EXPCUBE;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    public void DesMove()
    {
        Destroy(gameObject);
        //Debug.Log("OKDS");

    }
}
